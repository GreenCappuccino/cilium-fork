---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  creationTimestamp: null
  name: ciliumenvoyhttpfilters.cilium.io
spec:
  group: cilium.io
  names:
    categories:
    - cilium
    kind: CiliumEnvoyHTTPFilter
    listKind: CiliumEnvoyHTTPFilterList
    plural: ciliumenvoyhttpfilters
    shortNames:
    - cehf
    singular: ciliumenvoyhttpfilter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The age of the identity
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v2alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              httpFilters:
                description: HTTPFilters is a list of HTTPFilter to be inserted in
                  the HTTP connection manager filter chain
                items:
                  description: HTTPFilter is an Envoy extensions.filters.network.http_connection_manager.v3.HttpFilter
                  properties:
                    configDiscovery:
                      description: "ConfigDiscovery is a configuration source specifier
                        for an extension configuration discovery service. \n Warning:
                        Note that this is not validated extensively for now."
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    disabled:
                      description: Disabled, if set to true, makes the filter disabled
                        by default, and must be explicitly enabled by setting per
                        filter configuration in the route configuration.
                      type: boolean
                    isOptional:
                      description: IsOptional, if set to true, allows clients that
                        do not support this filter to ignore the filter but otherwise
                        accept the config. Otherwise, clients that do not support
                        this filter must reject the config.
                      type: boolean
                    name:
                      description: Name is the name of the filter configuration.
                      type: string
                    typedConfig:
                      description: TypedConfig is filter specific configuration which
                        depends on the filter being instantiated.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - name
                  type: object
                  x-kubernetes-validations:
                  - message: HTTPFilter must have exactly 1 of typedConfig or configDiscovery
                    rule: (has(self.typedConfig) || has(self.configDiscovery)) &&
                      !(has(self.typedConfig) && has(self.configDiscovery))
                type: array
            type: object
        required:
        - metadata
        type: object
    served: true
    storage: true
    subresources: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
